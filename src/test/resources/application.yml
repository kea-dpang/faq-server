spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dpang_faq?createDatabaseIfNotExists=true
    username: root
    password: 12345

  jpa:
    hibernate:
      ddl-auto: create

    show-sql: true
    
  cloud:
    discovery:
      enabled: false
